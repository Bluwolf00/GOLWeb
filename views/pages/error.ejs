<!doctype html>
<html lang="en" data-bs-theme="dark">

<head>
    <%- include('../partials/head', {pageTitle: "Error"}) %>
</head>

<%- include('../partials/headernav', {pageTitle: "Error", username: username}) %>

<body>
    <main>
        <div class="container my-5 text-center">

            <img id="front-logo" src="/img/logo_new.png" alt="">
            <h1 class="mb-5  ">Aw Crap!</h1>
            <% if (typeof error !== 'undefined') { %>
            <% if (error == 403) { %>
            <h2>Access Unauthorised</h2>
            <h3 class="lead">Woah there! Seems as though you've tried to tune into the command net. <br>Can't have that
                soldier!</h3>
            <% } else if (error == 404) { %>
            <h3>Woah there! That grid reference doesn't exist in this AO!</h3>
            <% } else { %>
            <h3 class="lead">Looks like you've got the wrong frequency!</h3>
            <% } %>
            <% } %>
            <p class="lead">No worries, try heading back to base for a debrief</p>
            <a href="/home"><button type="button" class="btn btn-primary btn-lg px-4 gap-3">RTB</button></a>

        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous">
        </script>

        <script defer>
            var isloggedIn = "<%= loggedin %>"
        </script>
        <script src="/scripts/checkLogged.js"></script>
    </main>

    <footer>
        <%- include('../partials/footer') %>
    </footer>
</body>

</html>